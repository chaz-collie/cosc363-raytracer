Ray Tracer made for a university assignment.
Received A+ (20/20)

Run the program by opening the terminal and navigating to the directory the files are stored in,
then use the following commands:
cmake .
make
./RayTracer.out
